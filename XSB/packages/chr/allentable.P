:- export cons_tri/3.

cons_tri(before, before, before).
cons_tri(before, after, before).
cons_tri(before, after, after).
cons_tri(before, after, during).
cons_tri(before, after, contains).
cons_tri(before, after, overlaps).
cons_tri(before, after, overlapped_by).
cons_tri(before, after, meets).
cons_tri(before, after, met_by).
cons_tri(before, after, starts).
cons_tri(before, after, started_by).
cons_tri(before, after, finishes).
cons_tri(before, after, finished_by).
cons_tri(before, after, equals).
cons_tri(before, during, before).
cons_tri(before, during, during).
cons_tri(before, during, overlaps).
cons_tri(before, during, meets).
cons_tri(before, during, starts).
cons_tri(before, contains, before).
cons_tri(before, overlaps, before).
cons_tri(before, overlapped_by, before).
cons_tri(before, overlapped_by, during).
cons_tri(before, overlapped_by, overlaps).
cons_tri(before, overlapped_by, meets).
cons_tri(before, overlapped_by, starts).
cons_tri(before, meets, before).
cons_tri(before, met_by, before).
cons_tri(before, met_by, during).
cons_tri(before, met_by, overlaps).
cons_tri(before, met_by, meets).
cons_tri(before, met_by, starts).
cons_tri(before, starts, before).
cons_tri(before, started_by, before).
cons_tri(before, finishes, before).
cons_tri(before, finishes, during).
cons_tri(before, finishes, overlaps).
cons_tri(before, finishes, meets).
cons_tri(before, finishes, starts).
cons_tri(before, finished_by, before).
cons_tri(after, before, before).
cons_tri(after, before, after).
cons_tri(after, before, during).
cons_tri(after, before, contains).
cons_tri(after, before, overlaps).
cons_tri(after, before, overlapped_by).
cons_tri(after, before, meets).
cons_tri(after, before, met_by).
cons_tri(after, before, starts).
cons_tri(after, before, started_by).
cons_tri(after, before, finishes).
cons_tri(after, before, finished_by).
cons_tri(after, before, equals).
cons_tri(after, after, after).
cons_tri(after, during, after).
cons_tri(after, during, during).
cons_tri(after, during, overlapped_by).
cons_tri(after, during, met_by).
cons_tri(after, during, finishes).
cons_tri(after, contains, after).
cons_tri(after, overlaps, after).
cons_tri(after, overlaps, during).
cons_tri(after, overlaps, overlapped_by).
cons_tri(after, overlaps, met_by).
cons_tri(after, overlaps, finishes).
cons_tri(after, overlapped_by, after).
cons_tri(after, meets, after).
cons_tri(after, meets, during).
cons_tri(after, meets, overlapped_by).
cons_tri(after, meets, met_by).
cons_tri(after, meets, finishes).
cons_tri(after, met_by, after).
cons_tri(after, starts, after).
cons_tri(after, starts, during).
cons_tri(after, starts, overlapped_by).
cons_tri(after, starts, met_by).
cons_tri(after, starts, finishes).
cons_tri(after, started_by, after).
cons_tri(after, finishes, after).
cons_tri(after, finished_by, after).
cons_tri(during, before, before).
cons_tri(during, after, after).
cons_tri(during, during, during).
cons_tri(during, contains, before).
cons_tri(during, contains, after).
cons_tri(during, contains, during).
cons_tri(during, contains, contains).
cons_tri(during, contains, overlaps).
cons_tri(during, contains, overlapped_by).
cons_tri(during, contains, meets).
cons_tri(during, contains, met_by).
cons_tri(during, contains, starts).
cons_tri(during, contains, started_by).
cons_tri(during, contains, finishes).
cons_tri(during, contains, finished_by).
cons_tri(during, contains, equals).
cons_tri(during, overlaps, before).
cons_tri(during, overlaps, during).
cons_tri(during, overlaps, overlaps).
cons_tri(during, overlaps, meets).
cons_tri(during, overlaps, starts).
cons_tri(during, overlapped_by, after).
cons_tri(during, overlapped_by, during).
cons_tri(during, overlapped_by, overlapped_by).
cons_tri(during, overlapped_by, met_by).
cons_tri(during, overlapped_by, finishes).
cons_tri(during, meets, before).
cons_tri(during, met_by, after).
cons_tri(during, starts, during).
cons_tri(during, started_by, after).
cons_tri(during, started_by, during).
cons_tri(during, started_by, overlapped_by).
cons_tri(during, started_by, met_by).
cons_tri(during, started_by, finishes).
cons_tri(during, finishes, during).
cons_tri(during, finished_by, before).
cons_tri(during, finished_by, during).
cons_tri(during, finished_by, overlaps).
cons_tri(during, finished_by, meets).
cons_tri(during, finished_by, starts).
cons_tri(contains, before, before).
cons_tri(contains, before, contains).
cons_tri(contains, before, overlaps).
cons_tri(contains, before, meets).
cons_tri(contains, before, finished_by).
cons_tri(contains, after, after).
cons_tri(contains, after, contains).
cons_tri(contains, after, overlapped_by).
cons_tri(contains, after, met_by).
cons_tri(contains, after, started_by).
cons_tri(contains, during, during).
cons_tri(contains, during, contains).
cons_tri(contains, during, overlaps).
cons_tri(contains, during, overlapped_by).
cons_tri(contains, during, starts).
cons_tri(contains, during, started_by).
cons_tri(contains, during, finishes).
cons_tri(contains, during, finished_by).
cons_tri(contains, during, equals).
cons_tri(contains, contains, contains).
cons_tri(contains, overlaps, contains).
cons_tri(contains, overlaps, overlaps).
cons_tri(contains, overlaps, finished_by).
cons_tri(contains, overlapped_by, contains).
cons_tri(contains, overlapped_by, overlapped_by).
cons_tri(contains, overlapped_by, started_by).
cons_tri(contains, meets, contains).
cons_tri(contains, meets, overlaps).
cons_tri(contains, meets, finished_by).
cons_tri(contains, met_by, contains).
cons_tri(contains, met_by, overlapped_by).
cons_tri(contains, met_by, started_by).
cons_tri(contains, starts, contains).
cons_tri(contains, starts, overlaps).
cons_tri(contains, starts, finished_by).
cons_tri(contains, started_by, contains).
cons_tri(contains, finishes, contains).
cons_tri(contains, finishes, overlapped_by).
cons_tri(contains, finishes, started_by).
cons_tri(contains, finished_by, contains).
cons_tri(overlaps, before, before).
cons_tri(overlaps, after, after).
cons_tri(overlaps, after, contains).
cons_tri(overlaps, after, overlapped_by).
cons_tri(overlaps, after, met_by).
cons_tri(overlaps, after, started_by).
cons_tri(overlaps, during, during).
cons_tri(overlaps, during, overlaps).
cons_tri(overlaps, during, starts).
cons_tri(overlaps, contains, before).
cons_tri(overlaps, contains, contains).
cons_tri(overlaps, contains, overlaps).
cons_tri(overlaps, contains, meets).
cons_tri(overlaps, contains, finished_by).
cons_tri(overlaps, overlaps, before).
cons_tri(overlaps, overlaps, overlaps).
cons_tri(overlaps, overlaps, meets).
cons_tri(overlaps, overlapped_by, during).
cons_tri(overlaps, overlapped_by, contains).
cons_tri(overlaps, overlapped_by, overlaps).
cons_tri(overlaps, overlapped_by, overlapped_by).
cons_tri(overlaps, overlapped_by, starts).
cons_tri(overlaps, overlapped_by, started_by).
cons_tri(overlaps, overlapped_by, finishes).
cons_tri(overlaps, overlapped_by, finished_by).
cons_tri(overlaps, overlapped_by, equals).
cons_tri(overlaps, meets, before).
cons_tri(overlaps, met_by, contains).
cons_tri(overlaps, met_by, overlapped_by).
cons_tri(overlaps, met_by, started_by).
cons_tri(overlaps, starts, overlaps).
cons_tri(overlaps, started_by, contains).
cons_tri(overlaps, started_by, overlaps).
cons_tri(overlaps, started_by, finished_by).
cons_tri(overlaps, finishes, during).
cons_tri(overlaps, finishes, overlaps).
cons_tri(overlaps, finishes, starts).
cons_tri(overlaps, finished_by, before).
cons_tri(overlaps, finished_by, overlaps).
cons_tri(overlaps, finished_by, meets).
cons_tri(overlapped_by, before, before).
cons_tri(overlapped_by, before, contains).
cons_tri(overlapped_by, before, overlaps).
cons_tri(overlapped_by, before, meets).
cons_tri(overlapped_by, before, finished_by).
cons_tri(overlapped_by, after, after).
cons_tri(overlapped_by, during, during).
cons_tri(overlapped_by, during, overlapped_by).
cons_tri(overlapped_by, during, finishes).
cons_tri(overlapped_by, contains, after).
cons_tri(overlapped_by, contains, contains).
cons_tri(overlapped_by, contains, overlapped_by).
cons_tri(overlapped_by, contains, met_by).
cons_tri(overlapped_by, contains, started_by).
cons_tri(overlapped_by, overlaps, during).
cons_tri(overlapped_by, overlaps, contains).
cons_tri(overlapped_by, overlaps, overlaps).
cons_tri(overlapped_by, overlaps, overlapped_by).
cons_tri(overlapped_by, overlaps, starts).
cons_tri(overlapped_by, overlaps, started_by).
cons_tri(overlapped_by, overlaps, finishes).
cons_tri(overlapped_by, overlaps, finished_by).
cons_tri(overlapped_by, overlaps, equals).
cons_tri(overlapped_by, overlapped_by, after).
cons_tri(overlapped_by, overlapped_by, overlapped_by).
cons_tri(overlapped_by, overlapped_by, met_by).
cons_tri(overlapped_by, meets, contains).
cons_tri(overlapped_by, meets, overlaps).
cons_tri(overlapped_by, meets, finished_by).
cons_tri(overlapped_by, met_by, after).
cons_tri(overlapped_by, starts, during).
cons_tri(overlapped_by, starts, overlapped_by).
cons_tri(overlapped_by, starts, finishes).
cons_tri(overlapped_by, started_by, after).
cons_tri(overlapped_by, started_by, overlapped_by).
cons_tri(overlapped_by, started_by, met_by).
cons_tri(overlapped_by, finishes, overlapped_by).
cons_tri(overlapped_by, finished_by, contains).
cons_tri(overlapped_by, finished_by, overlapped_by).
cons_tri(overlapped_by, finished_by, started_by).
cons_tri(meets, before, before).
cons_tri(meets, after, after).
cons_tri(meets, after, contains).
cons_tri(meets, after, overlapped_by).
cons_tri(meets, after, met_by).
cons_tri(meets, after, started_by).
cons_tri(meets, during, during).
cons_tri(meets, during, overlaps).
cons_tri(meets, during, starts).
cons_tri(meets, contains, before).
cons_tri(meets, overlaps, before).
cons_tri(meets, overlapped_by, during).
cons_tri(meets, overlapped_by, overlaps).
cons_tri(meets, overlapped_by, starts).
cons_tri(meets, meets, before).
cons_tri(meets, met_by, finishes).
cons_tri(meets, met_by, finished_by).
cons_tri(meets, met_by, equals).
cons_tri(meets, starts, meets).
cons_tri(meets, started_by, meets).
cons_tri(meets, finishes, during).
cons_tri(meets, finishes, overlaps).
cons_tri(meets, finishes, starts).
cons_tri(meets, finished_by, before).
cons_tri(met_by, before, before).
cons_tri(met_by, before, contains).
cons_tri(met_by, before, overlaps).
cons_tri(met_by, before, meets).
cons_tri(met_by, before, finished_by).
cons_tri(met_by, after, after).
cons_tri(met_by, during, during).
cons_tri(met_by, during, overlapped_by).
cons_tri(met_by, during, finishes).
cons_tri(met_by, contains, after).
cons_tri(met_by, overlaps, during).
cons_tri(met_by, overlaps, overlapped_by).
cons_tri(met_by, overlaps, finishes).
cons_tri(met_by, overlapped_by, before).
cons_tri(met_by, overlapped_by, during).
cons_tri(met_by, meets, starts).
cons_tri(met_by, meets, started_by).
cons_tri(met_by, meets, equals).
cons_tri(met_by, met_by, after).
cons_tri(met_by, starts, during).
cons_tri(met_by, starts, overlapped_by).
cons_tri(met_by, starts, finishes).
cons_tri(met_by, started_by, after).
cons_tri(met_by, finishes, met_by).
cons_tri(met_by, finished_by, met_by).
cons_tri(starts, before, before).
cons_tri(starts, after, after).
cons_tri(starts, during, during).
cons_tri(starts, contains, before).
cons_tri(starts, contains, contains).
cons_tri(starts, contains, overlaps).
cons_tri(starts, contains, meets).
cons_tri(starts, contains, finished_by).
cons_tri(starts, overlaps, before).
cons_tri(starts, overlaps, overlaps).
cons_tri(starts, overlaps, meets).
cons_tri(starts, overlapped_by, during).
cons_tri(starts, overlapped_by, overlapped_by).
cons_tri(starts, overlapped_by, finishes).
cons_tri(starts, meets, before).
cons_tri(starts, met_by, met_by).
cons_tri(starts, starts, starts).
cons_tri(starts, started_by, starts).
cons_tri(starts, started_by, started_by).
cons_tri(starts, started_by, equals).
cons_tri(starts, finishes, during).
cons_tri(starts, finished_by, before).
cons_tri(starts, finished_by, overlaps).
cons_tri(starts, finished_by, meets).
cons_tri(started_by, before, before).
cons_tri(started_by, before, contains).
cons_tri(started_by, before, overlaps).
cons_tri(started_by, before, meets).
cons_tri(started_by, before, finished_by).
cons_tri(started_by, after, after).
cons_tri(started_by, during, during).
cons_tri(started_by, during, overlapped_by).
cons_tri(started_by, during, finishes).
cons_tri(started_by, contains, contains).
cons_tri(started_by, overlaps, contains).
cons_tri(started_by, overlaps, overlaps).
cons_tri(started_by, overlaps, finished_by).
cons_tri(started_by, overlapped_by, overlapped_by).
cons_tri(started_by, meets, contains).
cons_tri(started_by, meets, overlaps).
cons_tri(started_by, meets, finished_by).
cons_tri(started_by, met_by, met_by).
cons_tri(started_by, starts, starts).
cons_tri(started_by, starts, started_by).
cons_tri(started_by, starts, equals).
cons_tri(started_by, started_by, started_by).
cons_tri(started_by, finishes, overlapped_by).
cons_tri(started_by, finished_by, contains).
cons_tri(finishes, before, before).
cons_tri(finishes, after, after).
cons_tri(finishes, during, during).
cons_tri(finishes, contains, after).
cons_tri(finishes, contains, contains).
cons_tri(finishes, contains, overlapped_by).
cons_tri(finishes, contains, met_by).
cons_tri(finishes, contains, started_by).
cons_tri(finishes, overlaps, during).
cons_tri(finishes, overlaps, overlaps).
cons_tri(finishes, overlaps, starts).
cons_tri(finishes, overlapped_by, after).
cons_tri(finishes, overlapped_by, overlapped_by).
cons_tri(finishes, overlapped_by, met_by).
cons_tri(finishes, meets, meets).
cons_tri(finishes, met_by, after).
cons_tri(finishes, starts, during).
cons_tri(finishes, started_by, after).
cons_tri(finishes, started_by, overlapped_by).
cons_tri(finishes, started_by, met_by).
cons_tri(finishes, finishes, finishes).
cons_tri(finishes, finished_by, finishes).
cons_tri(finishes, finished_by, finished_by).
cons_tri(finishes, finished_by, equals).
cons_tri(finished_by, before, before).
cons_tri(finished_by, after, after).
cons_tri(finished_by, after, contains).
cons_tri(finished_by, after, overlapped_by).
cons_tri(finished_by, after, met_by).
cons_tri(finished_by, after, started_by).
cons_tri(finished_by, during, during).
cons_tri(finished_by, during, overlaps).
cons_tri(finished_by, during, starts).
cons_tri(finished_by, contains, contains).
cons_tri(finished_by, overlaps, overlaps).
cons_tri(finished_by, overlapped_by, contains).
cons_tri(finished_by, overlapped_by, overlapped_by).
cons_tri(finished_by, overlapped_by, started_by).
cons_tri(finished_by, meets, meets).
cons_tri(finished_by, met_by, contains).
cons_tri(finished_by, met_by, overlapped_by).
cons_tri(finished_by, met_by, started_by).
cons_tri(finished_by, starts, overlaps).
cons_tri(finished_by, started_by, contains).
cons_tri(finished_by, finishes, finishes).
cons_tri(finished_by, finishes, finished_by).
cons_tri(finished_by, finishes, equals).
cons_tri(finished_by, finished_by, finished_by).
cons_tri(equals, before, before).
cons_tri(equals, after, after).
cons_tri(equals, during, during).
cons_tri(equals, contains, contains).
cons_tri(equals, overlaps, overlaps).
cons_tri(equals, overlapped_by, overlapped_by).
cons_tri(equals, meets, meets).
cons_tri(equals, met_by, met_by).
cons_tri(equals, starts, starts).
cons_tri(equals, started_by, started_by).
cons_tri(equals, finishes, finishes).
cons_tri(equals, finished_by, finished_by).
cons_tri(equals, equals, equals).
cons_tri(before, equals, before).
cons_tri(after, equals, after).
cons_tri(during, equals, during).
cons_tri(contains, equals, contains).
cons_tri(overlaps, equals, overlaps).
cons_tri(overlapped_by, equals, overlapped_by).
cons_tri(meets, equals, meets).
cons_tri(met_by, equals, met_by).
cons_tri(starts, equals, starts).
cons_tri(started_by, equals, started_by).
cons_tri(finishes, equals, finishes).
cons_tri(finished_by, equals, finished_by).
